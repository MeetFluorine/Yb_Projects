var element;
// will give whole structure;
// element= document;
// will give all structure in the form of array
// element= document.all;
// to target specific element from array
element= document.all[6];
console.log(element);